from turtle import *

def draw_triangle():
    forward(length)
    left(120)
    forward(length)
    left(120)
    forward(length)

length  = 150

draw_triangle()

right(120)
forward(length)
right(120)

draw_triangle()
